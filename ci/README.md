# Publish workflow (provenance-ready)

Intended install path: `.github/workflows/publish.yml`.

Copy:

```bash
cp ci/publish.yml .github/workflows/publish.yml
```

Note: updating `.github/workflows/*` via the ianalloway gh OAuth token requires the `workflow` scope; until then, apply this file via the GitHub UI or a token that has that scope.
